const base_url = "https://adsbot-api.herokuapp.com/dashboard/"; // production

const session = `${base_url}sessions`;
const registration = `${base_url}registrations`;
const password = `${base_url}passwords`;

export default { base_url, session, registration, password };
